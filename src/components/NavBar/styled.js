import styled  from 'styled-components';

const navbarheading = styled.h1`
    color:#082937 !important;
    font-family: 'Damion', cursive;
`;
const navlink = styled.span`
    cursor: pointer;
`;

export default { navbarheading, navlink};