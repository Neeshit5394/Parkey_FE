import styled from "styled-components";

const Container = styled.div`
  width: 50%;
  margin: 50px auto;
  .card {
    margin-bottom: 50px;
    border-radius: 0;
  }
`;

export default { Container };
